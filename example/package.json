{
	"name": "example",
	"scripts": {
		"dev": "turbo run dev --filter=example-host",
		"dev:guests": "turbo run dev --filter=example_guest --filter=example-guest_2",
		"dev:host": "turbo run dev --filter=example-host",
		"build": "turbo run build --filter=example-host --filter=example_guest --filter=example-guest_2"
	},
	"devDependencies": {
		"turbo": "^2.9.16"
	}
}
